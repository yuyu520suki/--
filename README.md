# RC Frame Optimization System
基于遗传算法的钢筋混凝土框架结构优化系统

## 项目简介
本项目是一个针对钢筋混凝土（RC）框架结构的自动化优化设计系统。它利用遗传算法（Genetic Algorithm）在满足中国国家规范（GB 55001-2021, GB 50010-2010, GB 50011-2010）的前提下，寻找造价最低的截面配置方案。

**当前版本**: v2.4 (自适应策略版) - 2026年2月1日更新

**状态**: ✅ 核心功能完成，全部验证通过

## 核心功能

### 1. 智能优化核心
- **遗传算法引擎**: 基于 `PyGAD` 库，采用改进的遗传策略
- **6基因分组编码**: 标准梁、屋面梁、底层柱、标准角柱、标准内柱、顶层柱
- **自适应策略**: 
  - 自适应惩罚系数λ (1.0→0.80)：前期严格约束，后期允许边界探索
  - 自适应变异率Pm (0.30→0.10→0.45)：根据收敛状态动态调整
- **多进程并行计算**: 采用 `multiprocessing.Pool` 突破GIL限制，实测加速比 **5.7x**

### 2. 精确结构分析
- **有限元分析**: 集成 `anaStruct` 进行2D框架分析
- **荷载模拟**: 恒载、活载、风荷载、雪荷载、地震作用
- **荷载组合**: 自动生成 SLS/ULS 组合 (1.3G+1.5L 等，符合 GB 55001-2021)
- **地震计算**: GB 50011-2010 底部剪力法

### 3. 工程合规性验算
- **规范标准**: GB 55001-2021, GB 50010-2010, GB 50011-2010, GB 50009-2012
- **验算项目**: P-M曲线、斜截面受剪、轴压比、配筋率、强柱弱梁

### 4. 验证模块
- 全局平衡检查、对称性检查、变形协调性检查、蒙特卡洛基准测试

### 5. GUI 界面
- 参数配置、实时预览、结果展示、一键生成报告
- **智能填充**: 根据建筑类型（住宅/办公/商场等）和地区自动填充规范值
- **并行加速开关**: 支持多进程并行(6进程)，获5.7倍加速

### 6. 自动化报告
- Word 计算书、Excel 报表、框架内力图、P-M曲线图、收敛曲线
- **水平荷载效应图**（地震/风荷载分布）

## 性能数据

| 指标 | 数值 |
|------|------|
| 搜索空间 | 77^6 = 2,084亿种组合 |
| 串行运行时间 | 134.9秒 |
| 并行运行时间 | 23.6秒 (6进程) |
| 加速比 | **5.7x** |
| 造价降幅 | **37.7%** (相比初始方案) |
| 对比经验设计 | 节省 **40%** |

## 快速开始

### 环境依赖
```bash
pip install pygad anastruct matplotlib pandas python-docx openpyxl numpy
```

### 运行程序
```bash
# 命令行优化
python main.py

# GUI 界面
python run_gui.py
```

## 项目结构
```
Project_Root/
├── src/
│   ├── models/          # 数据模型
│   │   ├── data_models.py         # GridInput, OptimizationResult
│   │   ├── structure_model.py     # 结构建模与分析
│   │   └── load_combinations.py   # 荷载组合生成 (GB 55001-2021)
│   ├── calculation/     # 力学计算
│   │   ├── capacity_calculator.py # P-M曲线、承载力计算
│   │   └── section_database.py    # 截面数据库
│   ├── optimization/    # 遗传算法
│   │   ├── optimizer.py           # GA 优化器核心 (含自适应策略)
│   │   └── parallel_worker.py     # 多进程并行工作模块
│   ├── analysis/        # 验证系统
│   │   ├── analyzer.py            # 截面验证器
│   │   ├── model_validator.py     # 综合验证主程序
│   │   └── ...                    # 各项检查模块
│   ├── utils/           # 报表工具
│   │   └── report_generator.py    # Excel/Word/图表自动生成
│   └── gui/             # GUI 界面
│       └── gui_main.py            # Tkinter 主界面
├── output/              # 优化结果输出目录
├── main.py              # 命令行入口
└── run_gui.py           # GUI 启动脚本
```

## 更新日志

### 2026-02-01 (v2.4 自适应策略版)
- **自适应惩罚系数**: λ从1.0动态调整至0.80，平衡可行性与经济性
- **自适应变异率**: Pm根据收敛状态在0.10-0.50间动态调整，防止早熟收敛
- **性能提升**: 并行加速比从2.8x提升至 **5.7x** (134.9s → 23.6s)
- **参数敏感性分析**: 新增种群规模和迭代代数敏感性测试功能
- **进度显示增强**: 同时显示变异率Pm和惩罚系数λ

### 2026-01-23 (v2.3 进程并行版)
- **多进程并行计算**: 采用 `multiprocessing.Pool` 突破Python GIL限制
  - 新增 `parallel_worker.py` 并行工作模块
  - 每个工作进程独立初始化模型实例，避免序列化问题

### 2026-01-17 (v2.2 规范核查版)
- **GB 55001-2021 全面核查**: 分项系数γG=1.3, γQ=1.5
- **水平荷载效应图**: 新增地震/风荷载分布可视化

### 2026-01-14 (v2.0 重构版)
- 项目结构重构，统一 import 路径
- GB 55001-2021 合规，多核并行计算支持

## 规范依据
- GB 55001-2021 工程结构通用规范
- GB 50010-2010 混凝土结构设计规范
- GB 50009-2012 建筑结构荷载规范
- GB 50011-2010 建筑抗震设计规范

## 许可证
MIT License
